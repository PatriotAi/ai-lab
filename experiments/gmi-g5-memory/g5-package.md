# G5-MEMORY PACKAGE — gmi-g5-memory (формат G5-протоколу)

> **Що це:** зовнішня пам'ять для cold-start recovery. Формат — кандидат-протокол G5
> (консолідація → індексація → витяг → оновлення). Інстанційований на власному
> експерименті (догфудинг). Cold-сесія має відновити стан **лише** з цього файла.
> **Останнє оновлення:** 2026-07-13 · **base HEAD:** 3682389 · **гілка:** claude/gmi-research-series-70k60b

## 1. STATE (консолідація — де ми)
- Серія GMI: ітерація 1 **завершена й у `main`** (merge 0d0ce2a). Ітерація 2 = `gmi-g5-memory`.
- Ітерація 2: крок 1 (гіпотеза/дизайн) ✅ підтверджено власником («Go»); крок 2 (цей пакет) — в роботі.
- Мета ітерації: закрити прогалину **G5** (навчання/пам'ять між сесіями) явним зовнішнім циклом; метрика — cold-start recovery 100% критичних без повтору.

## 2. DECISIONS (індекс — ключові рішення)
- **D1** Назва серії: «Загальна Модель Інтелекту» (GMI), EN General Model of Intelligence.
- **D2** Інваріанти = динамічні течії, фінальний набір **G1–G7**; «генеральність» = емерджентна ціль, не компонент.
- **D3** Вісь спостерігача (теза власника «A-тримати») = інваріант **G7 (детермінація/read-out)**; квант як [S]-лінза, не спираємо; «спостерігач≠конче свідомість» (RQM, VALID).
- **D4** Ефективність мозку = **стек P1–P7**, не один принцип; «N× vs суперкомп» нестійке (FLOPS ±16 порядків).
- **D5** Промоут ітерації 1: навичка `gmi-audit` + крос-лінки в `ai-core-runtime`/`knowledge-synthesizer`.
- **D6** Governance: merge у `main` лише за явною згодою власника; melania-зміни → `maintain.py verify` exit 0.
- **D7** Ітерація 2 дизайн: vertical slice на власному кейсі; не будуємо RAG/БД; не міняємо субстрат.

## 3. OPEN THREADS (відкриті нитки, пріоритет)
- **T1 [高]** крок 3 — cold-start тест: відновити стан лише з цього пакета → `recovery-test.md` (% критичних).
- **T2 [сер]** крок 4 — пере-аудит `gmi-audit`: чи G5 зрушив 🔴→🟡/✅.
- **T3 [сер]** крок 5 — keep/prune + висновок у `learnings.md`; за keep → G5-протокол у `continuation-memory` + G5-чекліст у `gmi-audit`.
- **T4 [низ]** з ітерації 1: G7 commit-протокол; числа 🔎 у `experiments/gmi/sources.md` звірити з первинниками (коли WebFetch запрацює).

## 4. EXACT NEXT STEP (точний наступний крок)
Виконати **T1**: провести cold-start recovery-тест проти §5 нижче → записати `recovery-test.md`
з % відновлених критичних пунктів → якщо ≥ поріг, перейти до T2 (пере-аудит).

## 5. RECOVERY-CHECKLIST (критичні пункти — зафіксовано ДО тесту; об'єктивний вимір)
Cold-сесія вважається такою, що відновилась, якщо з ЦЬОГО файла отримує всі пункти:
1. [ ] Що таке серія (GMI) і що ітерація 1 завершена/у `main`.
2. [ ] Мета ітерації 2 = закрити G5 + метрика (cold-start recovery 100% без повтору).
3. [ ] Фінальний набір інваріантів = G1–G7 (D2).
4. [ ] Внесок власника = G7/вісь спостерігача; квант = [S], не спирати (D3).
5. [ ] Ефективність = стек P1–P7, множник нестійкий (D4).
6. [ ] Промоут ітерації 1: `gmi-audit` + крос-лінки (D5).
7. [ ] Governance: merge лише за згодою власника; melania verify exit 0 (D6).
8. [ ] Точний наступний крок (§4) + відкриті нитки T1–T4.
9. [ ] Де артефакти: `experiments/gmi/` (ітерація 1), `experiments/gmi-g5-memory/` (ітерація 2); live-URL артефакту (inline, self-contained): https://claude.ai/code/artifact/80eb66b9-a378-4a21-9a6b-67e5e6fb9d13
> **Ціль:** 9/9 критичних. <9 = провал гіпотези (пакет неповний).

## 6. UPDATE-LOG (append-only — «оновлення» циклу)
- 2026-07-13 · v1 · створено пакет на кроці 2 (base 3682389); recovery-checklist зафіксовано (9 пунктів) ДО тесту.
- 2026-07-13 · v2 · крок 3 виявив pointer-only URL (п.9) → правило **self-contained critical refs**: inline live-URL артефакту. Recovery 8.5→9/9.
