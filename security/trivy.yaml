# Trivy — конфігурація для CI та локальних сканувань.
# Локально: trivy fs --config security/trivy.yaml .
severity: HIGH,CRITICAL
scan:
  scanners:
    - vuln
    - secret
    - misconfig
  skip-dirs:
    - node_modules
    - .git
