# skills/ — каталог навичок

Тут лежить **версіонований source** навичок лабораторії. Навичка (skill) — це
інструкція-автоматизація для Claude Code: коли і як виконати повторюване завдання.

## Як це працює
- **Канонічне джерело:** `.claude/skills/<name>/SKILL.md` — версіоноване в git
  (рев'ю, історія) і водночас активне: Claude Code підхоплює навички звідти
  (проєктні) або з `~/.claude/skills/` (глобальні).
- **Цей каталог (`skills/`)** — індекс: перелік активних навичок і правила створення.

## Формат
Кожна навичка = тека з `SKILL.md`. Шаблон — [`../templates/skill.md`](../templates/skill.md).
Ключове — поле `description`: **чіткий тригер**, коли навичку застосовувати.

## Активні навички
- `skill-new` — фабрика навичок: `.claude/skills/skill-new/SKILL.md`. Виклик: `/skill-new`.
- `translate-uaen` — двомовна локалізація UA⇄EN з глосарієм. Виклик: `/translate-uaen`.
- `experiment` — запуск і ведення експерименту за методологією. Виклик: `/experiment`.
- `content-pipeline` — контент від теми до двомовного артефакту. Виклик: `/content-pipeline`.
- `publish` — текст → Canva-картка → прев'ю на Vercel. Виклик: `/publish`.
- `weekly-review` — щотижнева ретроспектива за фактами. Виклик: `/weekly-review`.

## Нові навички
Каталог roadmap-навичок вичерпано — усі активні. Нові створюємо через `/skill-new`
(повторюване завдання → навичка → запис у `docs/learnings.md`).
