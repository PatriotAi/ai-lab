# Ретроспектива · 2026-07-04 (перший звіт: 2026-06-13 → сьогодні)

> Перший прогін навички `weekly-review` (живий тест). Далі — щотижнево.

## Факти періоду
12 комітів · 57 файлів · +1814 рядків · 7 записів у `learnings` ·
фази PLAN: 0 ✅ · 1 ✅ · 2 🟡 (код ✅, лишились ручні дії в GitHub UI) · 3 ✅ · 4 ✅

## ✅ Зроблено
- **Фундамент + білінгвальність** (Фази 0–1): структура, методологія, PLAN,
  глосарій, `docs/ua`+`docs/en`, CI безпеки/якості/залежностей.
- **Двигун продуктивності** (Фаза 3): 6 активних навичок — `skill-new`,
  `translate-uaen`, `experiment`, `content-pipeline`, `publish`, `weekly-review` —
  і SessionStart-хук (`automations/session-start/` + `.claude/settings.json`).
- **Перший наскрізний експеримент** (Фаза 4): `experiments/e2e-content-pipeline/` —
  тема → UA/EN артефакт → Canva-картка за ~22 хв одним проходом.
- **Max-effort рев'ю перед PR**: 15 знахідок → 10 виправлень (найцінніше —
  «невидимий» `.env.example`, стійкий `setup.sh`, concurrency у CI).

## 💡 Вивчено
- Хук = детермінованість: контекст щосесії без «якщо модель згадає».
- Рев'ю ловить «працює лише в автора» (зламаний онбординг свіжого клона).
- «Одна автоматизація — одна відповідальність» — правило довело користь на практиці.
- Фабрика навичок дає консистентний стандарт за хвилини на навичку.

## 🧱 Застрягло
- **Vercel-прев'ю**: немає `VERCEL_TOKEN`/CLI у середовищі — крок publish чесно
  відкладений (локальний HTML-знімок готовий).
- **Branch protection + code/secret scanning**: потрібні ручні дії в GitHub
  Settings (⛔ поза доступом агента).
- Session-ліміт API обірвав 4/10 рев'ю-агентів — верифікацію завершено в
  головному контурі без втрати покриття.

## Пріоритети наступного тижня
1. **P0** — Merge PR; одразу після: branch protection на `main` + увімкнути
   code/secret scanning (Settings → Security).
2. **P0** — `VERCEL_TOKEN` у середовище (або git-інтеграція Vercel) → довершити
   publish-крок експерименту живим прев'ю.
3. **P1** — Поставити `weekly-review` на розклад (щопонеділка 09:00).
4. **P1** — Другий експеримент: контент-пайплайн на зовнішній темі
   (з повним `/deep-research`) — перевірка двигуна на «чужому» матеріалі.
5. **P2** — Відкладені знахідки рев'ю: кеш semgrep у CI, доля локального
   gitleaks-хука.
