# CONTEXT — поточний стан лабораторії ai-lab

> Швидкий орієнтир «де ми зараз і як продовжити». Деталі: `PLAN.md` (фази),
> `learnings.md` (висновки), `roadmap.md` (беклог), `methodology.md` (цикл),
> `glossary.md` (терміни). Канонічна мова — українська; EN — дзеркало.

## Що це
Особиста **лабораторія продуктивності**: багатомовні (UA/EN) проєкти та AI-автоматизації.
Цикл: **гіпотеза → дизайн → запуск → вимір → лиши/прибери → висновок**.

## Стан фаз (з `PLAN.md`)
- Фаза 0 — Фундамент і безпека ✅
- Фаза 1 — Білінгвальний шар (UA-first) ✅
- Фаза 2 — Довершення безпеки/CI 🟡 (код ✅; branch protection ⛔ — потребує дії власника)
- Фаза 3 — Двигун продуктивності (навички/автоматизації) ✅
- Фаза 4 — Перший наскрізний експеримент ✅
- Фаза 5 — Дослідницька серія GMI ✅

## Активні дослідження / серії
- **GMI (Загальна Модель Інтелекту)** — `experiments/gmi/`: 7 інваріантів G1–G7 + вісь
  спостерігача + стек ефективності; двомовний інтерактивний Claude Artifact. Промоутнуто
  навичку **`gmi-audit`** (GMI-лінза). Відкритий **PR #16** (merge — за згодою власника).

## Governance (коротко)
- Пріоритет: **явний запит власника → правила лабораторії (`CLAUDE.md`) → протокол melania → SKILL.md**.
- **merge будь-якого PR у `main` — лише за явною згодою власника.** Пре-схвалення melania діє
  максимум до *відкриття PR*.
- melania-екосистема: перед комітом змін — `maintain.py verify` (exit 0); після свідомого
  оновлення — `resync` → `verify`. Downgrade/видалення навички — стоп і спитати власника.
- Жодних секретів у репо (лише env); перед комітом — `pre-commit` (`scripts/setup.sh`).

## Як продовжити роботу
1. Прочитай цей файл + останній запис у `learnings.md`.
2. Звір активну гілку розробки та скоуп інструментів (змінюються між сесіями).
3. Для нового зрізу — навичка `experiment` (скаффолд у `experiments/`).
4. Замкни цикл висновком у `learnings.md`; онови `PLAN.md`/`CONTEXT.md`, якщо щось промоутнув.

## Останнє оновлення
2026-07-13 — додано Фазу 5 (серія GMI) + навичку `gmi-audit`; створено цей файл.
