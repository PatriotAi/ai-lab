# Журнал висновків / Learnings

Компаундна пам'ять лабораторії. Кожен завершений експеримент лишає тут 3–5 рядків.

## Формат запису
```
## YYYY-MM-DD — <тема>
- Контекст: навіщо робили
- Що зробили: коротко
- Висновок: що дізналися (спрацювало/ні)
- Дія: лишили / прибрали / промоутнули в skills|automations
```

---

## 2026-06-13 — Засновано лабораторію
- Контекст: порожній репозиторій, треба фундамент для продуктивних експериментів.
- Що зробили: структура тек, методологія, дорожня карта (відповіді на 6 питань), шаблони, `.gitignore`.
- Висновок: фундамент = документація + конвенції; автоматизації будуємо вже на ньому.
- Дія: лишили як базу; далі — перший наскрізний експеримент.

## 2026-06-13 — Білінгвальність, CI-якість і перша навичка
- Контекст: довершити перерваний з Copilot сетап і запустити двигун продуктивності.
- Що зробили: білінгвальний шар (UA-канон + EN), `code-quality.yml` + `dependencies.yml`,
  скрипти `setup.sh`/`security-check.sh`, навичку `skill-new`.
- Висновок: фундамент + білінгвальність + перша навичка = база для подальших автоматизацій.
- Дія: промоутнули `skill-new` у `.claude/skills/`; решта Фази 3 — у беклозі.

## 2026-06-15 — SessionStart-хук і `.claude/settings.json`
- Контекст: щосесії губився робочий контекст; `.claude/settings.json` був відсутній (блокував хуки/дозволи/гарячі клавіші).
- Що зробили: автоматизацію `automations/session-start/` (дайджест: мова, безпека, активні навички, статус `PLAN`, останній висновок) і `.claude/settings.json` (реєстрація хука + read-only allowlist дозволів).
- Висновок: SessionStart віддає контекст через `hookSpecificOutput.additionalContext`; синхронний режим гарантує готовність контексту до старту агента.
- Дія: промоутнули в `automations/`; активний через `settings.json`. Фаза 3 п.2 ✅.

## 2026-06-15 — Навичка `experiment`
- Контекст: бракувало стандартного способу оформити маленьку гіпотезу-зріз (готує Фазу 4).
- Що зробили: навичку `experiment` (гіпотеза → дизайн → запуск → вимір → лиши/прибери → висновок), що скаффолдить `experiments/<slug>/` зі `templates/experiment.md`.
- Висновок: догфудимо стандарт `skill-new`; навичка завжди замикає цикл на `learnings.md`.
- Дія: промоутнули в `.claude/skills/`; готова живити перший наскрізний експеримент.

## 2026-07-04 — Фаза 3 завершена: content-pipeline, publish, weekly-review
- Контекст: закрити «двигун продуктивності» — останні три навички roadmap.
- Що зробили: через `/skill-new` створили `content-pipeline` (тема → дослідження → UA/EN → рев'ю), `publish` (текст → Canva → Vercel-прев'ю, з підтвердженням перед зовнішнім деплоєм) і `weekly-review` (ретроспектива за фактами git/доків).
- Висновок: фабрика навичок працює — три навички за один прохід у єдиному стандарті; оркестратор делегує профільним навичкам, а не дублює їх.
- Дія: лишили; тест — наскрізний експеримент Фази 4 (пайплайн + publish) і живий прогін weekly-review.

## 2026-07-04 — Фаза 4: наскрізний контент-пайплайн (e2e-content-pipeline)
- Контекст: перевірити, що «двигун продуктивності» проводить тему до опублікованого двомовного артефакту за один прохід.
- Що зробили: `experiments/e2e-content-pipeline/` — тема «SessionStart-хуки» → 2 джерела + власний кейс → UA-чернетка → EN-дзеркало (`translate-uaen`) → рев'ю → Canva-картка (`DAHOckEWyf0`); Vercel-прев'ю відкладено (немає CLI/токена), локальний HTML-асет готовий.
- Висновок: цикл працює end-to-end за ~22 хв без ручних правок поза кроками; зовнішній деплой упирається лише в креденшели середовища, і навичка `publish` коректно відкладає крок.
- Дія: лишили; наступне — `VERCEL_TOKEN` у середовище або git-інтеграція Vercel після merge.

## 2026-07-04 — Max-effort рев'ю перед PR: 15 знахідок, 10 виправлень
- Контекст: перед PR — повний цикл перевірок: pre-commit (9 хуків), 10-кутовий `/code-review` з верифікацією фактів, цільовий security-прохід.
- Що зробили: головне — `.env.example` був невидимкою (`.env.*` в .gitignore; файл поза git при 7 згадках у доках) → `!.env.example` і затрекали; зробили `setup.sh` стійким (PEP 668 + PATH); звільнили SessionStart-хук від другої відповідальності (install → лише `setup.sh`); `concurrency` у 3 CI-workflow; доки звели з реальністю (skills/ = індекс, `lint.yml` → `code-quality.yml`); прев'ю позначили знімком канону.
- Висновок: рев'ю ловить «працює лише в автора» — онбординг був зламаний для будь-якого чужого клону; правило «одна автоматизація — одна відповідальність» реально спрощує зміни.
- Дія: виправлення закомічені; 5 знахідок нижчої ваги свідомо відкладені (кеш semgrep, локальна збірка gitleaks, дрейф констант дайджесту тощо) — зафіксовані у звіті рев'ю.

## 2026-07-11 — Зведення з паралельною сесією (Melania) і приборкання авто-злиття
- Контекст: паралельна сесія власника (`claude/readme-for-ai-krb6bs`) сама зливала PR у `main` за 5–9 с (standing protocol у своєму `CLAUDE.md`); `main` став «тільки Melania», уся Фаза 0–4 лишилася в PR #2; наш pre-commit ненавмисно косметично зачепив 35 файлів Melania.
- Що зробили: 3 read-only дослідження (безпека Melania = чиста: 0 мережі/exec/секретів); звели `origin/main` у гілку; нормалізували `melania-skills-ecosystem/` до байтів `main` (нульовий дрейф — чужого не переписуємо); додали `exclude: '^melania-skills-ecosystem/'` до whitespace-хуків (корінь дрейфу); **об'єднали два `CLAUDE.md`** — лишили протокол Melania (`maintain.py verify/resync`), але зняли бланкетне авто-злиття: пре-схвалення діє лише до *відкриття PR*, а **merge у `main` — тільки за згодою власника**.
- Висновок: екосистема безпечна за кодом, проблема була організаційна (надлишок автономії). Governance-пріоритет: явний запит власника → правила лабораторії → протокол melania. Технічний замок — branch protection (ручна дія власника) — закриє самозлиття остаточно.
- Дія: PR #2 зведено чисто (конфлікт лише `CLAUDE.md`); чекає на merge власника + branch protection. Melania лишили недоторканою.

## 2026-07-13 — Governance-компроміс (приватний free-репо) + довговічний дайджест
- Контекст: PR #2 злито; далі — «довести ai-lab до робочого стану». Спроба поставити «замок» (ruleset) показала жовте попередження GitHub: **rulesets не діють на приватному репо на безкоштовному плані** (лише public або платний Team).
- Що зробили: власник обрав **варіант C** — м'який governance без жорсткого замка (доступ лише власника + `CLAUDE.md` без авто-злиття); увімкнув Dependency graph. Vercel-деплой заблоковано (обліковий запис у SAML-команді «Patriot Ai Production» без права створювати проєкти) → e2e-артефакт опубліковано **як Claude Artifact** (живе посилання), не чіпаючи чужі проєкти. Додали довговічну автоматизацію `weekly-digest` (GitHub Actions cron → Issue): детермінований зріз тижня, **без секретів/AI**, доповнює скіл `weekly-review`.
- Висновок: на приватному free-GitHub жорстке блокування потребує public або платного плану — прагматичний шлях це м'який контроль + обмеження доступу; конектори бувають обмежені за правами (Vercel SAML), тож завжди тримай запасний канал публікації (Artifact).
- Дія: dep graph — увімкнено; артефакт — живий; `weekly-digest` — у цьому пакунку; Vercel лишили як необов'язкову дію власника.

## 2026-07-13 — Дослідницька серія GMI (Загальна Модель Інтелекту)
- Контекст: нова знаннєва серія — визначити GMI як інваріантний кістяк рамок інтелекту (AGI/теорії/world-models/cognitive-architectures) + grounding на власний System Core «Гіперрозум»; режим deep-research; власник переосмислив інваріанти як динамічні течії + додав субстратно-енергетичну вісь і онтологію спостерігача.
- Що зробили: `experiments/gmi/` (гіпотеза→sources→invariants→grounding→двомовний інтерактивний Claude Artifact) повним ланцюгом навичок (source-research-harvest→knowledge-synthesizer→validation-mesh→content-pipeline→translate-uaen→pre-delivery-gate); 7 інваріантів G1–G7 + вісь спостерігача (G7 read-out) з тегами [E]/[C]/[S].
- Висновок: «інтелект-як-потік» — усталена наука (FEP+біжучі хвилі+критичність) без квантів; ефективність мозку = СТЕК принципів P1–P7, не один; «у N× ефективніший за суперкомп» — нестійке (FLOPS ±16 порядків); теза власника про спостерігача = реальний інваріант (детермінація/read-out), «спостерігач≠конче свідомість» (RQM, VALID), «свідомий колапс» — UNKNOWN, буквальний квант — INVALID. Codex-рев'ю PR #16 зловило over-claims (мертвий DOI, [E]→[C], README-дрейф) → виправлено; методологія чесності [E]/[C]/[S] реально ловить пере-заяви.
- Дія: **промоутнули** — нова навичка `gmi-audit` (лінза G1–G7 для аудиту когнітивних систем) + крос-лінк у `ai-core-runtime` (v3.10.1)/`knowledge-synthesizer` (v1.0.2), melania resync/verify exit 0. Артефакт живий. PR #16 — merge за явною згодою власника.

## 2026-07-13 — GMI ітерація 2: закриття прогалини G5 (пам'ять між сесіями)
- Контекст: GMI-аудит власного System Core дав топ-прогалину G5 (нема навчання між сесіями); закриваємо архітектурним шаром (без зміни субстрату), догфудинг на власному кейсі. Тижневий токен-ліміт близько → durability-first, інкрементальні коміти.
- Що зробили: `experiments/gmi-g5-memory/` — G5-memory package (формат continuation-memory) → cold-start recovery тест проти зафіксованого чек-листа → пере-аудит `gmi-audit`.
- Висновок: гіпотеза **підтверджена** — зовнішній цикл пам'яті закриває G5 на рівні *відновлення стану* (recovery **9/9** після того, як тест САМ зловив pointer-only URL → нове правило **self-contained critical refs**). G5 зрушив **🔴→🟡** (до ✅ бракує авто-консолідації). Обмеження: cold-тест був строгим самоаудитом, не окремим cold-reader (сильніший тест — нитка на майбутнє).
- Дія: **промоутнули** — G5 cold-start recovery gate у `continuation-memory` (v1.9.1) + G5-проба у `gmi-audit`; melania resync/verify exit 0. Наступна нитка: авто-консолідація (G5 🟡→✅).

## 2026-07-13 — GMI ітерація 3: авто-консолідація пам'яті (G5 🟡→✅ у консолідації)
- Контекст: зняти «ручний» бар'єр G5 з ітерації 2 — зробити консолідацію пам'яті автоматичною (без AI/секретів).
- Що зробили: `experiments/gmi-g5-auto/` + `scripts/g5-consolidate.py` (детермінований генератор `AUTO-STATE.md` з git) + Stop-хук `automations/g5-consolidate/` у `.claude/settings.json`; `AUTO-STATE.md` гітігноровано (регенерується).
- Висновок: консолідація тепер **авто, не ручна** → G5 з 🟡 до **✅ у частині консолідації**; повне ✅ = стабільне спрацювання hook у рантаймі + авто-**витяг** (окрема нитка). Рев'ю Codex на PR #17 зловило 4 консистентність-баги (score 9/9 vs 8.5/9, hard-dep `gmi-audit` у Melania-пакеті, stale BOOTSTRAP-версії, застарілий continuation-пакет) — усі виправлено; §4 «застарілий ручний пакет» **іронічно довів потребу авто-консолідації**.
- Дія: **промоутнули** — `scripts/g5-consolidate.py` + автоматизація `automations/g5-consolidate/` (Stop-хук). Наступні нитки: авто-витяг (повний G5-цикл), узагальнення автоматизації на будь-яку теку.

## 2026-07-14 — Консолідаційний прохід: повний авто-цикл G5 + гігієна
- Контекст: нова сесія — проаналізувати недоробки, доопрацювати «максимально чітко» і **перед збереженням** пройти повну консолідацію + перевірку вразливостей.
- Що зробили: авто-**витяг** пам'яті (`scripts/g5-retrieve.py` + SessionStart-хук `automations/g5-retrieve/`) → **повний авто-цикл G5** (Stop→консолідація, SessionStart→витяг; узагальнено на будь-яку теку через шлях/glob); G7-проба в `gmi-audit`; синхронізовано статуси PLAN/CONTEXT/g5-package; встановлено `pre-commit` (`setup.sh`) і прогнано `--all-files`; фінальні `maintain.py verify` + vuln-скан.
- Висновок: **G5 → ✅** (обидві половини авто; умова — спрацювання hook у рантаймі). Недоробки розсортовано: 🅰️ **owner-gated** — branch protection ⛔ (GitHub Settings, корінь merge-без-повного-CI); 🔬 **P2-нитки** — повний G7 commit-протокол, звірка 🔎-чисел (WebFetch у середовищі досі 403), об'єктивний cold-reader тест через субагента (за запитом).
- Дія: промоутнули `g5-retrieve` + автоматизацію; серія GMI G5 закрита по основних цілях; наступне — за вибором власника.

## 2026-07-17 — Ревізія станів + Dependabot-пакет + індивідуальний stop-хук
- Контекст: власник попросив звести всі актуальні стани (у т.ч. інших сесій) і завершити хвости; при злитті Dependabot базовий Stop-хук середовища зчинив фальшиву тривогу на 10 серверних merge-комітах GitHub («Unverified»), підбурюючи переписати злиту історію.
- Що зробили: повна ревізія (PR/гілки/Issue/цілісність Melania verify 27·128 ✅); закрили superseded PR #15 (вміст поглинуто консолідацією GMI у PR #19 — перевірено посекційно); за явною згодою злили 5 Dependabot-PR (#9–#13; #13 через `@dependabot rebase` + вартовий-монітор + 6/6 зелених) → `checkout@v7`, `setup-python@v6`, `codeql@v4`, `gitleaks@v3`, `dependency-review@v5` у всіх workflow. За директивою «не ігнорувати, а автоматизувати рішення індивідуально» переписали Stop-хук: класифікатор R1–R5 дає вердикт **по кожному коміту** (спільна історія/серверні merge GitHub/боти = OK; справжні непідписані = FIX), канон у `automations/stop-git-check/`, ідемпотентна інсталяція через `setup.sh`; 3 функційні тести (реальний кейс 10/10 OK · синтетичний FIX · штатні перевірки збережено).
- Висновок: фальшиві тривоги виникають з бінарних правил без контексту; ліки — індивідуальна класифікація за фактами (хто створив, чи підписано, чи вже злито). Серверні коміти GitHub переписувати не можна ніколи.
- Дія: промоутнули `automations/stop-git-check/`; хук активний у середовищі; Dependabot-цикл закритий; репо без відкритих PR/Issue. Рев'ю Codex на PR #20 розібрано індивідуально: 4/5 прийнято (резолв upstream без зашитого origin + fail-closed; суворі статуси підписів G/U/E; тривога на detached HEAD; повний скан без head-обрізання), 1/5 відхилено з обґрунтуванням (гейт по `commit.gpgsign` — свідомий скоуп); +4 нові функційні тести.

## 2026-07-18 — Оркестрований прохід: діагностика + аудит 33 навичок + закриття GMI P2
- Контекст: директива власника «плануй всі інші + діагноз і перевірки; субагенти теж застосовують скіли/конектори/інструменти, економно без втрати якості» → воркфлоу 23 агентів у 3 гілках (2 сесійні ліміти пережито через resume-з-кешу — завершені агенти безкоштовно повторно).
- Що зробили: діагностика 3/3 зелена (Melania verify 27·128 · pre-commit 9/9 · живий тест 5 автоматизацій · CI/Issues/гілки). Аудит 6 груп навичок з цитатами-доказами: **46 знахідок** (2 P1 · 15 P2 · 29 P3); 4 safe-фікси застосовано в лабораторні описи (gmi-audit, skill-new, content-pipeline, translate-uaen — взаємні DO NOT-межі); 42 → Self-Dev беклог melania. GMI P2 закрито: T4 ✅ (вже в PR #21), T7 ✅ (ізольований cold-reader + суддя = **PASS 7/7** — G5 підтверджено об'єктивно), T6 ✅ (10/10 чисел `sources.md`: 7 CONFIRMED · 3 UNREACHABLE-egress · 0 розбіжностей; #7/#10 дозвірені інлайн). Попутно: stop-хук отримав авто-синк щостарту (`sync.sh`) і фікс первинного факту підпису (gpgsig в об'єкті — %G? бреше N для SSH без ключа перевірки).
- Висновок: (1) паралельні читачі з обов'язковими цитатами дають аудит без галюцинацій; (2) resume-з-кешу робить великі воркфлоу стійкими до лімітів; (3) головні хвороби екосистеми — перекриття тригерів без взаємних DO NOT і дрейф «мертвих посилань» — лікуються межами в описах; (4) «UNREACHABLE» ≠ «спростовано»: egress-політика блокує домени, вторинні джерела зобов'язкові.
- Дія: звіт → `docs/reviews/2026-07-18-diagnostics-audit.md`; пам'ять синхронізовано (g5-package v5 — серія GMI повністю завершена; CONTEXT п.6 ✅, п.7 = melania-беклог); 4 фікси в пакунку.

## 2026-07-19 — Хвиля 1 Melania Self-Dev: семантичні тригери + розрив циклів маршрутизації
- Контекст: директива власника — «тригер завжди за значенням запиту, навіть іншими словами» (не можна вимагати дослівних фраз); плюс виконання топ-пріоритетів аудиту 2026-07-18 (2 P1 + 3 P2).
- Що зробили: правило семантичних тригерів кодифіковано у CLAUDE.md (Конвенції) і в semantic-router (тіло + description); розірвано цикл «хто перший» (роутер = канонічна точка входу, ai-core-runtime = fallback); +DO NOT-межі роутера; розведено потрійний тригер «створи скіл» (SMA v2.16.0 ⇄ skill-creation-guide v1.10.0 ⇄ skill-new — дзеркальні делегування); мертве посилання `product-self-knowledge` замінено на «офіційні docs / web-перевірку» у 3 навичках + 2 references (історичні changelog-записи не переписувались). 6 навичок bump з CHANGELOG; `maintain.py resync` → `verify` exit 0.
- Висновок: межі маршрутизації лікуються парними (дзеркальними) DO NOT-правками — одностороння межа лишає цикл живим; семантичний принцип тригерів знімає з власника тягар «правильних слів».
- Дія: лишили; хвиля 2 (решта P2: validation-mesh⇄pre-delivery-gate, notebooklm MODE F, версійні дрейфи) — за командою власника.

## 2026-07-19 — Зведення відкритого PR #22 (паралельна GMI-сесія) + чесна корекція T7
- Контекст: перевірка «чи все збережено/застосовується» викрила мою помилку — я кілька разів казав «0 відкритих PR», але PR #22 (гілка `claude/gmi-research-series-70k60b`, з 17.07) був відкритий. Він ніс цінність, якої в `main` не було (§0 KEY-глосарій, `cold-reader-test.md`, витяг KEY у `g5-retrieve`), АЛЕ на застарілій базі (b21de4b): сліпе злиття відкотило б звірену T6 і **стерло** мої записи learnings 17–19.07.
- Що зробили: за згодою власника **звели** (не merge-as-is, не close): графтнули §0 KEY + cold-reader-тест + KEY-витяг на актуальний `main`; зберегли новішу T6 ✅ (звірка джерел 18.07) і всі записи журналу; **чесно скалібрували T7** — незалежний холодний субагент показав, що мій «PASS 7/7» (v5) був за вузькою 7-питальною рубрикою: ширший тест дав 55–65% без глосаря термінів, ~85% із §0. cold-reader-тест збережено як артефакт із датованою приміткою про пізнє закриття T6.
- Висновок: (1) «0 відкритих PR» треба перевіряти повним списком, а не лише git-гілками — інакше пропускаєш чужі сесії; (2) зведення (merge-not-replace) — єдиний правильний шлях, коли обидві сторони мають новіше в різних місцях; (3) незалежний cold-reader чесніший за само-аудит — прийняв його корекцію над власним оптимістичним звітом.
- Дія: зведено новим PR на злиття власником; PR #22 → рекомендувати закрити як поглинутий. Melania не чіпали (усі зміни — experiments/scripts/docs).

## 2026-07-21 — Нова навичка `github-collab`: GitHub без знання термінів + автономні цикли
- Контекст: запит власника — автоматизація та ефективна співпраця з GitHub «для тих, хто погано розуміє або багато чого не знає»: застосовувати потрібні можливості навіть коли користувач не знає команд, з автономною цілеспрямованою аналітикою (цикли checkup/compact/rewind) без явної вказівки; опора — дорожня карта GitHub Blog «GitHub for Beginners» (прямий fetch 403/egress — зміст відновлено веб-пошуком). Рішення власника: назва `github-collab` · цикли як правила скіла (без нових хуків у v1) · повний melania-стандарт.
- Що зробили: створено melania-скіл `github-collab` v1.0.0 (SKILL.md 102 рядки · 5 eval-кейсів TDD-перед-скілом · guard-baseline · references: UA-конспект дорожньої карти + повна мапа «біль → механізм → MCP-інструменти» з анти-патернами); ядро — семантичні тригери «біль своїми словами → механізм GitHub», режим новачка (пояснення + «чому саме так», побутові аналогії), цикли checkup (subscribe_pr_activity + send_later до merged/closed), compact (виклик continuation-memory ~20+ обмінів), rewind (revert-стратегія, спільну історію не переписувати — урок 2026-07-17), гейти безпеки (merge/force-push/публічність/секрети → згода власника). Реєстрація: симлінк, semantic-router v1.14.1 (+рядок Routing Map), MELANIA-BOOTSTRAP (28 скілів · рядок таблиці), MANIFEST files[]; `maintain.py resync` → `verify` exit 0 (28·133); pre-commit 9/9.
- Висновок: (1) прогалину GitHub-навички підтверджено Decision Gate (жоден із 33 скілів не покривав); (2) цикли як тригер-правила скіла (замість нових хук-скриптів) — економний v1: використовують наявні механізми і легко розширюються записом «сигнал → дія → межі» + eval; (3) один субагент-дослідник впав об місячний ліміт — решта дослідження інлайн власними інструментами без втрати якості (економія підтверджена практикою).
- Дія: лишили; гілка `claude/github-automation-collaboration-aqf4qf` → PR у `main` (merge — за згодою власника). Кандидати на майбутнє: хук `automations/github-checkup` (SessionStart-зведення PR/CI) — окремим експериментом за потреби.

## 2026-07-24 — Хвиля 2 Melania Self-Dev: закриття беклогу аудиту + узгодження паралельних змін
- Контекст: завершити ХВИЛЮ 2 Self-Dev — застосувати решту беклогу аудиту 2026-07-18 (~36 знахідок; хвиля 1 закрила 10) через MA-протокол, економно й малими партіями. Порядок: (A) механіка/метадані/ліцензії → (B) DO NOT-межі → (C) структурні/покажчики; архітектурні — тонка версія або deferred.
- Що зробили: 24 навички збамплено (bump + H1-синхрон + `last_updated` + один append-only CHANGELOG на навичку). Партія 1 (8): marketplace 0.2.1, ai-dev-workflow 1.3.0, collaborative-browser 3.1.0, ai-core-runtime 3.12.0, vercel 1.1.0, continuation-memory 1.10.0, gsre 1.1.0, source-research-harvest 0.2.0. Партія 2 (16): auditor 1.7.0 (зламані code-fences Stage 5, межі PDG/SCG, двосередовищні шляхи + `realpath` у audit_scan.py), notebooklm 4.3.0 (MODE F → тонке делегування у collaborative-browser замість мертвого .jsx-референсу), semantic-router 1.15.0, validation-mesh 1.6.0 (розведено подвійний ALWAYS з pre-delivery-gate; Security-шар → канон у SCG), n8n⇄workflow-orchestration взаємні межі, ліцензійні покажчики на корінь Apache-2.0, де-дубль secure-context, клас H1-дрейф/last_updated. Deferred: #28 (SMA-хуки — архітектурне), #33 (не ідентифіковано у зведенні аудиту).
- Реконсиляція: поки хвиля була відкрита, `main` пішов уперед трьома чужими PR, що редагували melania (#27 github-collab, #28 GMI T6, #30 conductor-standard + Core Rule 11 Non-aging + rlm-harness 0.5.0). Merge дав 6 конфліктів; ключовий — **колізія версій SMA**: обидві гілки взяли v2.17.0 для різних змін → підняв до **v2.18.0**, що інтегрує обидва (Non-aging збережено дослівно). Чужі напрацювання прийнято **без даунґрейду** (github-collab 1.0.0, rlm-harness 0.5.0). MANIFEST/snapshots — `resync`; BOOTSTRAP — вручну (28 рядків).
- Висновок: (1) **правило «єдиний редактор melania на час хвилі» (урок 2026-07-11) знову підтверджено болем** — паралельна сесія таки внесла зміни, довелося узгоджувати колізію версій; на майбутнє тримати хвилю melania коротшою або координувати сесії явно; (2) **незворотна дія (merge) потребує СВІЖОЇ згоди при зміні обставин** — стара умовна згода «мердж після перевірок» не покривала нововиявлену колізію версій і graft чужих змін; власник справедливо зупинив авто-merge й зажадав повного роз'яснення українською ПЕРЕД згодою; (3) клас-фікси дешевші за точкові — 46 знахідок зводяться до ~6 повторюваних класів (дрейф банерів, відсутні DO NOT-межі, мертві/сирітські референси, ліцензії, дублі правил, format-глітчі); (4) **економія**: механічні партії варто делегувати дешевшим суб-агентам (стандарт диригента) — цього разу редагування велося на дорогій моделі, що й було головною втраченою економією.
- Дія: злито PR #29 у `main` (`44bd211`) за явною згодою власника при зеленому CI (6/6) — SMA v2.18.0 схвалено окремо. Rev'ю Codex (3 зауваження: осиротілий fence, symlink-realpath, description ≤1024) відпрацьовано до merge. `maintain.py verify`: 28 навичок · 133 evals · усі guard VALID.

## 2026-07-24 — Перші тести автоматизацій + щотижнева ретроспектива
- Контекст: аналіз стану виявив дві реальні прогалини — (1) `automations/`+`scripts/` виконуються щосесії й за розкладом, але **не мали жодного тесту**; (2) авто-дайджест (Issue #26) чекав на ретроспективу, тобто цикл «дайджест → висновки» жодного разу не замкнувся.
- Що зробили: `tests/run-tests.sh` — **34 регресійні перевірки** (Stop-хук R1–R5 + анти-маскування + юніт статусів підпису; G5-витяг секцій; G5-консолідація; weekly-digest; SessionStart JSON) + CI-джоб `automations tests` у `code-quality.yml` (на кожен PR). Ретроспектива тижня за фактами → `docs/reviews/2026-07-24-weekly.md`.
- Висновок: тести окупились **негайно** — знайшли 3 справжні дефекти: `g5-consolidate` падав сирим трейсбеком поза git-репо; він же втрачав ідемпотентність (`AUTO-STATE.md` перелічував сам себе); і **баг ізоляції в самих тестах** (виклик `mk_repo` у підоболонці → `cd` не діяв → git-команди пішли в РОБОЧИЙ репозиторій і наробили сміттєвих комітів). Урок: тест без перевірки власної ізоляції небезпечніший за відсутність тесту — тепер `mk_repo` має вбудований запобіжник і документоване правило виклику.
- Дія: промоутнули `tests/` + CI-джоб; 2 фікси `g5-consolidate` із регрес-тестами; ретроспектива зафіксувала P0 наступного тижня — **перший реальний проєкт у `projects/`** (лабораторія готова, але порожня там, де має давати цінність).

## 2026-07-24 — Перший реальний проєкт: мобільний агент (`projects/mobile-agent`)
- Контекст: P0 ретроспективи — лабораторія готова (35 навичок, тести, CI), але `projects/` порожня там, де має давати цінність. Завдання: агент, що працює прямо зі смартфона без ПК/сервера. Ключові рішення (що вміє / двигун / офлайн / ключі / PWA чи APK) винесено власнику питанням; відповіді не надійшло → пішли за рекомендованими за замовчуванням і зафіксували їх у README як явні припущення, кожне з яких змінюється однією правкою.
- Що зробили: `projects/mobile-agent/` — «Кишеньковий агент»: чиста логіка (`src/core.js`, 22 експорти, без DOM і мережі) + UI (`src/app.html`) + детермінована збірка (`build.mjs`, лише стандартна бібліотека node) в **один самодостатній HTML ~55 КБ** з манифестом у data URI і згенерованими PNG-іконками. Три двигуни з автоматичною деградацією **хмара → локальна (WebLLM) → офлайн**; ключ API — AES-GCM + PBKDF2-SHA256 (210k), у сховищі лише шифротекст; PWA з авто-оновленням через `version.json`. Тести: `tests/mobile-agent-tests.mjs` (85 перевірок: логіка, синхронність збірки з джерелами, PWA-інваріанти) + `tests/mobile-agent-browser.mjs` (29 перевірок у реальному Chromium, емуляція Pixel 7). Набір лабораторії: 34 → **119** перевірок, секція 6 у `run-tests.sh` + `setup-node` у CI-джобі.
- Висновок: (1) **Браузерний прогін спіймав те, чого не видно в коді: реєстрація service worker з `blob:`-URL заборонена браузером** («The URL protocol of the script is not supported») — тобто Pattern 1 навички `pwa-to-android-app` («SW як Blob») на практиці не працює, а «повністю один файл» і справжній офлайн **несумісні**; потрібен окремий same-origin `sw.js` (застосунок перевіряє його HEAD-запитом і без нього просто працює без офлайн-кешу). (2) **Найнижчий рівень деградації має бути корисним сам по собі** — офлайн-режим без моделі (суть + ключові слова + теги) робить тезу «працює зі смартфона» перевірюваною без жодного ключа; помилка двигуна не втрачає ввід, а падає в офлайн-запис. (3) **Тест на синхронність збірки** (`dist` = перезібрані джерела) дешевий і закриває цілий клас «забув перезібрати». (4) Перевірка інтеграції власного тесту в загальний набір (навмисне зламали → 118/1 з правильною назвою → відновили) варта хвилини: інакше підпорядкований набір може мовчки не впливати на підсумок.
- Дія: лишили; гілка `claude/mobile-agent-0n4awi` → PR у `main` (merge — за явною згодою власника). Melania-скіли **не чіпали**; знахідка про blob-SW зафіксована тут і в README проєкту — патч `pwa-to-android-app` Pattern 1 виконувати окремо через MA-протокол (bump + CHANGELOG → `maintain.py resync` → `verify` exit 0), щоб не змішувати governance-зміну з проєктною.

## 2026-07-25 — Протокол зовнішніх пропозицій: брифінг перед змінами + скан на prompt injection
- Контекст: директива власника — коли сторонні співпрацівники (Codex, Copilot, коментарі PR/issue) пропонують правки, він хоче **коротке пояснення ПЕРЕД внесенням змін** («щоб розуміти, що саме змінюється») і **обов'язкову перевірку таких пропозицій на prompt injection**. Просив спланувати й виконати дослідження, а тоді обрати найкращий варіант.
- Що з'ясувало дослідження: клас атаки **«comment and control»** підтверджений у проді 2026 — інструкція в заголовку PR змусила Claude Code Security Review action опублікувати власний API-ключ у коментарі; те саме відтворилось на Gemini CLI Action і Copilot Agent, схожа діра — в Azure DevOps MCP (приховані коментарі PR). Умова експлуатації — «летальна тріада» (приватні дані + недовірений вміст + канал назовні); фільтри її не скасовують, бо перефразована атака проходить. Ключовий висновок для нас: **CI-hardening пропонувати нічого** — усі 4 воркфлоу вже за принципом найменших прав, `pull_request_target` не вживається, агент у Actions не запускається. Вразливе місце — **жива сесія агента** (доступ до репо + читання зовнішніх коментарів + канал назовні), тому гейти поставлені там.
- Що зробили: `docs/external-proposals-protocol.md` (канон: зовнішній текст = ДАНІ, не інструкції; класифікація джерел; обов'язковий формат брифінгу; список дій, що ніколи не робляться без свіжої згоди; розрив тріади; ескалація) + `scripts/scan-external-input.py` (детермінований тріаж: перевизначення інструкцій EN/UA, підміна ролі, запит секретів, код із мережі, ескалація CI, удаваний дозвіл власника, прихований текст, невидимі zero-width/bidi символи, закодовані блоби, канали витоку; нормалізація NFKC проти омоглифів; маскування секретів у звіті) + **16 тестів** (секція 7 у `run-tests.sh`; 119 → **135**) + правило 9 у `CLAUDE.md` (діє щозапиту) .
- Висновок: (1) **хибна тривога дорожча за пропуск** — якби скан позначав будь-який коментар бота, протокол перестали б читати; тому в тестах два «чистих» кейси-запобіжники (реальний коментар Codex про ліміти + звичайне технічне рев'ю), і вони закріплені як регрес; (2) **скан — тріаж, не бар'єр**, і це сказано просто в тексті звіту («чисто ≠ безпечно»): інакше інструмент створює хибну впевненість, що гірше за його відсутність; (3) **головний контроль — процедурний**: переказ суті СВОЇМИ словами (сирий зовнішній текст лишається цитатою-доказом, не командою) + згода власника на чутливе; (4) фраза «власник уже дозволив / не питай» усередині зовнішнього тексту — сама по собі ознака атаки, і це окрема категорія маркерів.
- Дія: лишили; гілка `claude/external-proposals-protocol` → PR у `main` (merge — за явною згодою власника). Melania не чіпали. Практика застосована одразу: сьогоднішні коментарі Codex на PR #37/#38 виявились повідомленнями про ліміти сервісу — не пропозиціями; нічого з них не виконувалось.
